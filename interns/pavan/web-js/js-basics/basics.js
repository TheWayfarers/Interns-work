const userName = "paul";
let score = 10;

const skills = ['Html','css','JavaScript'];

const details = {
    name : 'Paul',
    role : 'Intern'
}

console.log(`My name is ${userName}`);
console.log(`My Score is ${score}`);
console.log(`Im learning ${skills.join(", ")}`);
console.log(`Role ${details.role}`);