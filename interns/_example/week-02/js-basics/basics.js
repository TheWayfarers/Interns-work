// Run with: node basics.js

// Variables
const name = "Intern";
let score = 0;
score = score + 10;

// Data types
const isLearning = true;
const topics = ["HTML", "CSS", "JavaScript", "React", "Node"];
const person = { name: "Alex", role: "Intern" };

// Operators
const total = 5 + 3;
const greeting = "Hello, " + name;

// Template literals
const message = `Hi ${name}! Your score is ${score}.`;

console.log(greeting);
console.log(message);
console.log("Topics:", topics.join(", "));
console.log("Person:", person.name, "-", person.role);
